input {
  http {
    port => 8080
    codec => json
    # basic auth, matches the Elastic backend's username/password config
    # user => "logflare"
    # password => "changeme"
  }
}

output {
  stdout {
    codec => rubydebug
  }
}
