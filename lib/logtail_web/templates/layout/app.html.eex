<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Logtail!</title>
    
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">

    <script>
      <%= if @conn.assigns.user do %>
        window.userToken = "<%= Phoenix.Token.sign(LogtailWeb.Endpoint, "key", @conn.assigns.user.id) %>"
      <% end %>
    </script>

  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="nav">
          <ul class="nav nav-pills pull-right">
            <%= if @conn.assigns[:user] do %>
              <li>
                <%= link "Dashboard", to: source_path(@conn, :dashboard) %>
              </li>
              <li>
                <%= link "Logout", to: auth_path(@conn, :logout) %>
              </li>
            <% else %>
              <li>
                <%= link "Sign in with Github", to: auth_path(@conn, :request, "github") %>
              </li>
            <% end %>
          </ul>
        </nav>
        <h1>Log Tail</h1>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
