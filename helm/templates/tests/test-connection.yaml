apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "logflare.fullname" . }}-test-connection"
  labels:
    {{- include "logflare.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "logflare.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
